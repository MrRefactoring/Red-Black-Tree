{
  "name": "redBlackTree-node",
  "version": "1.0.1",
  "description": "The algorithm of a self-balancing tree is used (Red-Black tree)",
  "author": "Vladislav Tupikin",
  "license": "MIT",
  "repository": "https://github.com/MrRefactoring/redBlackTree-node",
  "main": "src/index.js",
  "directories": {
    "test": "test"
  },
  "scripts": {
    "start": "node src/index.js",
    "test": "qunit",
    "release": "git clean -dfx && npm config set production && npm version patch && npm publish && npm config set dev"
  },
  "devDependencies": {
    "node-cmd": "^3.0.0",
    "qunit": "^2.6.1"
  },
  "dependencies": {}
}
